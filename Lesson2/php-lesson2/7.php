<?php
	
	$a = mt_rand(-10, 10);
	$b = mt_rand(-10, 10);

	/*
		|| - или
		&& - и
	*/

	if(($a > 0) || ($b > 0)){
		echo ($a > $b) ? $a : $b;
	}
	else{
		echo 'пропускаем!';
	}

	/* перебор 
	echo ($a > 0) || ($b > 0) ? ($a > $b) ? $a : $b : 'пропускаем!';
	*/